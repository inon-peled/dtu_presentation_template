% !!!! THIS DOCUMENT SHOULD BE COMPILED WITH LuaLaTeX !!!!

\documentclass[10pt]{beamer} % 4:3 standard format
% \documentclass[aspectratio=169]{beamer} % uncomment for 16:9 wide format

\usetheme[pageofpages=of,% String used between the current page and the total page count.
          bullet=circle,% Use circles instead of squares for bullets.
          titleline=true,% Show a line below the frame title.
          alternativetitlepage=true,% Use the fancy title page.
          titlepagelogo=logo_dtu_mlsm,% Logo for the first page.
          watermark=logo_empty,% Watermark used in every page.
          watermarkheight=55px,% Height of the watermark.
          watermarkheightmult=4,% The watermark image is 4 times bigger than watermarkheight.
          ]{DTU}

\usepackage{fontspec}
% Fonts downloaded from http://legionfonts.com/fonts/neo-sans-pro
\setmainfont{NeoSansPro}[
    Extension=.ttf,
    BoldFont=NeoSansProBold,
    ItalicFont=NeoSansProItalic,
    BoldItalicFont=NeoSansProBoldItalic,
]
% use DTU fonts -- must compile with LuaLaTeX
\usefonttheme{professionalfonts} % using non standard fonts for beamer
\usefonttheme{serif} % default family is serif

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{bm}
\usepackage{graphicx}
\usepackage[
    giveninits=true,
    backend=biber,
    natbib=true,
    style=numeric,
    % maxbibnames=9,
    % maxcitenames=1,
]{biblatex}
\DeclareNameAlias{author}{family-given}
\addbibresource{references.bib}

% You can disable a compilation warning about "Patching footnotes failed" by uncommenting the following: 
% \usepackage{silence}
% \WarningFilter{biblatex}{Patching footnotes failed}

% For transitioning to new Section:
\newcommand{\transframe}[1]{
    \begin{frame}[c]{}
        \begin{center}
        \hrule \vskip 15pt
        \huge #1
        \vskip 15pt \hrule
        \end{center}
    \end{frame}
}
\newcommand{\secframe}[1]{
    \transframe{#1}
    \section{#1}
}

% For equations:
\newcommand\numberthis{\addtocounter{equation}{1}\tag{\theequation}}
\renewcommand{\vec}[1]{\bm{#1}}
\newcommand{\vectrans}[1]{\vec{#1}^{\mbox{\scriptsize T}}}

% For citations:
\newcommand{\citeall}[1]{
    \citeauthor{#1} 
    (\citeyear{#1}), 
    \citetitle{#1}
}
\newcommand{\acite}[1]{\autocite{#1}}

% Info for title page
\author{
    First Author,
    \textbf{Presenting Author},
    Third Author\texorpdfstring{$^\ddagger$}, ...}
\title{
    %\LARGE 
    Presentation Title}
\institute{Technical University of Denmark}
\date{\texorpdfstring{$^\ddagger$}\  Other Institute Affiliation \\[\bigskipamount]Conference and Date}

% start slides
\begin{document}

% title slide
\begin{frame}[t,plain]
\titlepage
\end{frame}

\secframe{Here Starts a New \emph{Section}}

\begin{frame}[t]{Frame Title}
Example equation \cite{dummycite}:

\begin{align}
    y = \vectrans{\beta} \vec{x} + \varepsilon
\end{align}
\end{frame}

\begin{frame}[t]{References}  % [allowframebreaks]
    \setbeamertemplate{bibliography item}[text]
    \renewcommand*{\bibfont}{\small}
    \printbibliography
\end{frame}

\end{document}
